name: Python application

on:
  push:
   branches: [ master ]
  pull_request:
    branches: [ master ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.DOCKERPW }}" | docker login -u "prathamkumars125" --password-stdin
        docker image build -t dab8106/pyflask:latest .
        docker push prathamkumars125/chat-with-multiple-documents:latest
